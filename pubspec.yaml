name: conectapro
description: App ConectaPro - conexão entre clientes e profissionais.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^4.6.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.2.0
  url_launcher: ^6.3.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # ✅ AQUI ESTÁ O LOGO
  assets:
    - assets/images/logo.png